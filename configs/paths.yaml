data:
  raw_data: "data/raw/Prostate_Cancer.xlsx"
  processed_data: "data/processed/prostate_cancer_processed.csv"

models:
  base_models: "models/trained_models"
  ensemble_models: "models/ensemble_models"

reports:
  figures: "results/figures"
  tables: "results/tables"
  predictions: "results/predictions"
  results: "results"

artifacts:
  training_summary: "results/training_summary.json"
  voting_model: "models/ensemble_models/soft_voting_ensemble.pkl"
  stacking_model: "models/ensemble_models/stacking_ensemble.pkl"
